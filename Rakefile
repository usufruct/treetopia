namespace :db do
    desc 'Creates a blank db'
    task :create do
        puts 'db:create'
    end

    desc 'Drop the db'
    task :drop do
        puts 'db:drop'
    end

    desc 'Migrate to the latest version'
    task :migrate do
        puts 'db:migrate'
    end

    desc 'Rollback the most recent migration'
    task :rollback do
        puts 'db:rollback'
    end
end
